/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any custom Next.js config here, for example:
  // images: { domains: ['your-image-domain.com'] },
  // env: { CUSTOM_VAR: process.env.CUSTOM_VAR },
};

module.exports = nextConfig;
