rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Match all files in teachers/ directory
    match /teachers/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Deny everything else
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
